/* ───────────────────────────────────────────────────────
   WaveformCanvas – V2 <canvas> waveform renderer (stub)
   Renders PCM data as a waveform inside a clip.
   ─────────────────────────────────────────────────────── */
import React from 'react';

export default function WaveformCanvas({ audioBuffer, width, height, color }) {
  // PHASE 3+ full implementation with offscreen rendering
  return <canvas className="v2-waveform-canvas" width={width} height={height} />;
}
