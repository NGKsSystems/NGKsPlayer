import React from 'react';

const BandExceptionsManager = ({ onNavigate }) => {
  return (
    <div className="band-exceptions-manager">
      <h3>Band Exceptions Manager</h3>
      <p><em>This feature is temporarily disabled during the rebuild.</em></p>
    </div>
  );
};

export default BandExceptionsManager;